import { initActivityFilter } from './activities.js';

document.addEventListener('DOMContentLoaded', () => {
    // Initialize the activity filter (only active on the Certifications page)
    initActivityFilter();
});
